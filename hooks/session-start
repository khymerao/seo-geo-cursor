#!/usr/bin/env bash
# sessionStart — inject context at chat session start (Cursor: additional_context).
set -euo pipefail

escape_for_json() {
  local s="$1"
  s="${s//\\/\\\\}"
  s="${s//\"/\\\"}"
  s="${s//$'\n'/\\n}"
  s="${s//$'\r'/\\r}"
  s="${s//$'\t'/\\t}"
  printf '%s' "$s"
}

context="Plugin SEO & GEO Skills is active. See skills/ and rules/ for agent guidance."

if [[ -n "${CURSOR_PLUGIN_ROOT:-}" ]]; then
  skill="$CURSOR_PLUGIN_ROOT/skills/seo-geo-cursor-skill/SKILL.md"
  if [[ -f "$skill" ]]; then
    context="${context}\n\nPrimary skill: seo-geo-cursor-skill"
  fi
fi

escaped=$(escape_for_json "$context")
printf '{\n  "additional_context": "%s"\n}\n' "$escaped"
exit 0
